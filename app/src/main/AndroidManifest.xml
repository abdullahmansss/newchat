<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.samar.chatroom">

    <application
        android:name="softagi.ss.chatroom.offline"
        android:allowBackup="true"
        android:icon="@mipmap/ic_add_chat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="softagi.ss.chatroom.ChatActivity"></activity>
        <activity
            android:name="softagi.ss.chatroom.AddRoomActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity android:name="softagi.ss.chatroom.MainActivity"></activity>
        <activity android:name="softagi.ss.chatroom.StartActivity" />
        <activity android:name="softagi.ss.chatroom.Login" />
        <activity
            android:name="softagi.ss.chatroom.RegisterActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>